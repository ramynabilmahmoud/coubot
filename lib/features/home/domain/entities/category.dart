class Category {
  final String id;
  final String title;
  final String iconKey; // e.g. "food", "drinks", "dessert"

  const Category({required this.id, required this.title, required this.iconKey});
}
